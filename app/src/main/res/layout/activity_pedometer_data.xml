<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/app_bg"
    android:orientation="vertical"
    tools:context=".Homepage.PedometerData">

    <TextView
        android:id="@+id/txtviewAnimalNumber"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="120dp"
        android:text="Animal Number / ID"
        android:textColor="@color/Black"
        android:textSize="20sp" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recPedoData"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/txtviewAnimalNumber"
        android:layout_marginTop="15dp"
        android:padding="10dp" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_alignBottom="@+id/recPedoData"
        android:padding="5dp"
        android:orientation="horizontal">

        <Button
            android:id="@+id/generateConclusion"
            android:layout_width="180dp"
            android:layout_height="wrap_content"
            android:layout_alignBottom="@+id/recAnimalDetails"
            android:padding="2dp"
            android:layout_marginHorizontal="5dp"
            android:text="Generate Conclusion"
            android:textSize="15sp" />

    </LinearLayout>


</RelativeLayout>